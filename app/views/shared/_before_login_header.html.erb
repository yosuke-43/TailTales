<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to root_path, class:'navbar-brand', data: { turbo_stream: true } do %>
      <%= image_tag 'logo.png', size: '80x80' %>
    <% end %>
    <a class="navbar-brand" href="#"></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to t('defaults.header.to_home'), root_path, class: 'nav-link', data: { turbo_stream: true } %>
        </li>
        <li class="nav-item">
          <%= link_to t('defaults.header.to_diagnoses'), new_diagnosis_path, class: 'nav-link', data: { turbo_stream: true } %>
        </li>
        <li class="nav-item">
          <%= link_to t('defaults.header.to_dogs_index'), "#", class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to t('defaults.header.to_login'), login_path, class: 'nav-link', data: { turbo_stream: true } %>
        </li>
        <li class="nav-item">
          <%= link_to t('boards.index.title'), boards_path, class: 'nav-link', data: { turbo_stream: true } %>
        </li>
      </ul>
    </div>
  </div>
</nav>